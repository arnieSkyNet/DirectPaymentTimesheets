# Architecture document moved

This filename is retained so older links do not break.

The authoritative and current architecture document is [ARCHITECTURE.md](ARCHITECTURE.md). Do not use this compatibility file as a separate architecture specification.
